'use strict';

/**
 * regime-alimentaire router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::regime-alimentaire.regime-alimentaire');
